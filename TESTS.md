# Test Documentation

- `package.json` is required for Node projects, and testing is done through Node.
- `puppeteer` and `jest` are required for automated tests. Install them by navigating to the `tests` directory and running `npm install`.

## Sources

- [Test Chrome Extensions with Puppeteer | Chrome for Developers](https://developer.chrome.com/docs/extensions/how-to/test/puppeteer)
